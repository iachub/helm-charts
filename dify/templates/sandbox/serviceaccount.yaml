{{- if .Values.sandbox.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dify.sandbox.serviceAccountName" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: sandbox
  {{- if .Values.sandbox.serviceAccount.annotations }}
  annotations:
    {{- include "common.tplvalues.render" (dict "value" .Values.sandbox.serviceAccount.annotations "context" $) | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.sandbox.serviceAccount.automountServiceAccountToken }}
{{- end }}
