apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dify.sandbox.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: plugin-daemon
data:
  {{- include "dify.sandbox.config" . | nindent 2 }}
