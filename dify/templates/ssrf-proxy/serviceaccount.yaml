{{- if .Values.ssrfProxy.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dify.ssrfProxy.serviceAccountName" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: ssrf-proxy
  {{- if .Values.ssrfProxy.serviceAccount.annotations }}
  annotations:
    {{- include "common.tplvalues.render" (dict "value" .Values.ssrfProxy.serviceAccount.annotations "context" $) | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.ssrfProxy.serviceAccount.automountServiceAccountToken }}
{{- end }}
