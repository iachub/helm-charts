apiVersion: v1
kind: Service
metadata:
  name: {{ include "dify.ssrfProxy.fullname" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: ssrf-proxy
    {{- if .Values.ssrfProxy.service.labels }}
    {{- include "common.tplvalues.render" (dict "value" .Values.ssrfProxy.service.labels "context" $) | nindent 4 }}
    {{- end }}
  {{- if .Values.ssrfProxy.service.annotations }}
  annotations:
    {{- include "common.tplvalues.render" (dict "value" .Values.ssrfProxy.service.annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  {{- if .Values.ssrfProxy.service.clusterIP }}
  clusterIP: {{ .Values.ssrfProxy.service.clusterIP }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.ssrfProxy.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "common.labels.matchLabels" . | nindent 4 }}
    app.kubernetes.io/component: ssrf-proxy
